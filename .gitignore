# Volume mirrors (the DataAcquistion repo's download.sh, or an aws s3 cp for a local run) — never commit
/data/
/data_nasdaq/
/data_tiingo/
# Other volume mirrors (borrow, calendar, finbert, M1 landing layer, whole-market panel)
/data_borrow/
/data_calendar/
/data_finbert/
/data_quality/
/m1/
/m1x/
.DS_Store
__pycache__/
*.pyc
.env

# --- prediction stack: generated / downloaded, never sourced from git ---
.pytest_cache/
derived_*/
artifacts/models/
artifacts/scores/
scratch*/
*.log
# reports stay tracked (small, reviewable): artifacts/reports/
# ledger/trials.parquet stays tracked (G-09 append-only audit trail)

# --- app ---
node_modules/
app/*/dist/
app/backend/data/          # paper-book runtime state
# reports live in MongoDB only — the predict pod publishes them from the volume
# (pod_bootstrap_predict.sh publish_bundle); nothing is kept here or in git
/reports/
