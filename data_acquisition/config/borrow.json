{
  "_comment": "D-10 borrow fees/availability. TWO sources. (1) IBKR usa.txt: today's indicative snapshot for ALL ~19.7k shortable names — the whole file is stored, not a universe slice, because universe membership is recomputed over time and a snapshot missed is a snapshot gone. (2) iBorrowDesk: a rolling ~1-year DAILY history per ticker, merged append-only so the rolling window accretes into permanent history. Source 2 softens spec G-05 — borrow history is NOT purely forward-accruing after all.",
  "countries": ["usa"],
  "iborrowdesk": {
    "enabled": true,
    "_stocks_from": "universe is shared with the EODHD config rather than duplicated here; one request per ticker at iborrowdesk_pace_sec, so 503 names take ~9 min",
    "stocks_from": "tickers.json"
  },
  "iborrowdesk_v2": {
    "enabled": true,
    "_comment": "ALL-TIME daily OHLC (fee/rebate/available, back to 2015-07) from the keyed v2 API on the Patreon $10+ allowance: 500 units/month, resetting the 1st UTC, 1 unit = 365 days of one symbol. Universe = intraday.json's research names (stocks + market) that THIS pass's iborrowdesk universe also holds: the main pass takes the 92 in tickers.json, watchlist_borrow.json the other 13. Rows merge into history/<T>.json with EXISTING rows winning; the raw payload (figi, previous_identities, full OHLC) lands in history_v2/<T>.json and is the per-symbol done marker. A spent allowance defers the rest to the first run after the reset and never fails the borrow run. Needs IBORROWDESK_API_KEY in runpod/.env.",
    "stocks_from": "intraday.json",
    "stocks_from_keys": ["stocks", "market"],
    "only_in_history_universe": true,
    "_overlap_free_window_days": "stop the request short of today at the fewest whole units whose span still overlaps the free endpoint's rolling year by this many days (those days are already in history/). null = all-time, one extra unit per symbol.",
    "overlap_free_window_days": 60
  }
}
