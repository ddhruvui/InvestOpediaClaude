# Copy to app/backend/.env for local runs (gitignored). On Vercel, set these as
# project environment variables instead.
MONGO_URI=mongodb+srv://<user>:<db_password>@<cluster-host>/?appName=Cluster0
DB_PASSWORD=your-atlas-password
# Optional
# MONGO_DB=InvestOpediaClaude
# REPORT_BUNDLE=latest
# CORS_ORIGIN=https://investopediaclaudefe.onrender.com
# PORT=8787
